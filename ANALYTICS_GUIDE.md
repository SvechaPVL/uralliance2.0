# 📊 Руководство по Аналитике Yandex Metrika

Полное руководство по всем целям и событиям, которые отслеживаются на сайте UrAlliance.

## 🎯 Обзор Целей

На сайте настроено **16 целей** Yandex Metrika, разделенных на 3 уровня по важности:

### **Tier 1: Критические Конверсии** (7 целей)

Самые важные действия пользователей, прямо влияющие на продажи:

| Цель                  | Название в Metrika    | Где срабатывает            | Параметры                     |
| --------------------- | --------------------- | -------------------------- | ----------------------------- |
| **Отправка формы**    | `contact_form_submit` | ContactForm                | `service`: "legal" или "tech" |
| **Начало заполнения** | `contact_form_start`  | ContactForm (первый фокус) | -                             |
| **Ошибка формы**      | `contact_form_error`  | ContactForm                | `error`: текст ошибки         |
| **Клик по телефону**  | `phone_click`         | Все tel: ссылки            | `phone`: номер телефона       |
| **Клик по email**     | `email_click`         | Все mailto: ссылки         | `email`: адрес                |
| **Клик WhatsApp**     | `whatsapp_click`      | Все WhatsApp кнопки        | `location`: где кликнули      |
| **Клик Telegram**     | `telegram_click`      | Все Telegram кнопки        | `location`: где кликнули      |

### **Tier 2: Микроконверсии** (2 цели)

Промежуточные действия, показывающие интерес:

| Цель               | Название в Metrika | Где срабатывает                 | Параметры             |
| ------------------ | ------------------ | ------------------------------- | --------------------- |
| **Hero CTA Legal** | `cta_hero_legal`   | Главная страница, левая кнопка  | `label`: текст кнопки |
| **Hero CTA Tech**  | `cta_hero_tech`    | Главная страница, правая кнопка | `label`: текст кнопки |

### **Tier 3: Вовлечение** (2 цели)

Взаимодействие с контентом:

| Цель                  | Название в Metrika    | Где срабатывает | Параметры                         |
| --------------------- | --------------------- | --------------- | --------------------------------- |
| **Фильтр прайса**     | `price_filter_change` | Страница /price | `filter`: "all", "legal", "tech"  |
| **Сортировка прайса** | `price_sort_change`   | Страница /price | `sort`: "featured", "asc", "desc" |

### **A/B Тесты** (5 целей)

Автоматически отслеживаются при назначении вариантов:

| Тест                       | Цели                                    | Варианты |
| -------------------------- | --------------------------------------- | -------- |
| **Hero CTA тексты**        | `ab_test_hero_cta_variant_A/B/C`        | A, B, C  |
| **Приоритет мессенджеров** | `ab_test_messenger_order_variant_A/B/C` | A, B, C  |

---

## 📍 Расположение Целей на Сайте

### **Главная Страница (Hero Section)**

- ✅ Hero CTA Legal - левая кнопка
- ✅ Hero CTA Tech - правая кнопка (2 кнопки)
- ✅ A/B тест текстов CTA

### **ContactForm (все страницы)**

- ✅ Начало заполнения (первый фокус)
- ✅ Успешная отправка
- ✅ Ошибка отправки

### **Header (все страницы)**

Пока не добавлено (может быть добавлено позже)

### **MobileMenu**

- ✅ Телефон
- ✅ Email

### **Footer (все страницы)**

- ✅ Telegram
- ✅ WhatsApp
- ✅ Email
- ✅ A/B тест приоритета мессенджеров

### **ContactCTA Section**

- ✅ Телефон
- ✅ Email
- ✅ WhatsApp
- ✅ Telegram

### **Страница /contacts**

- ✅ Telegram кнопки (hero)
- ✅ WhatsApp кнопки (hero)
- ✅ Телефон (карточки контактов + кнопки)
- ✅ Email (карточки контактов + кнопки)

### **Страница /price**

- ✅ Фильтры категорий (all/legal/tech)
- ✅ Сортировка (рекомендуемые/цена↑/цена↓)

---

## 🛠️ Техническая Реализация

### **Основной файл**

`src/lib/analytics.ts` - главная утилита с функциями отслеживания

### **Основные функции**

```typescript
// Базовая функция отслеживания
reachGoal(goalName: MetrikaGoal, params?: GoalParams): void

// Специализированные функции
trackPhoneClick(phoneNumber?: string): void
trackEmailClick(email?: string): void
trackMessengerClick(messenger: "whatsapp" | "telegram", location?: string): void
trackCTAClick(location: string, label?: string): void
```

### **Пример использования**

```typescript
import { reachGoal, trackPhoneClick } from "@/lib/analytics";

// Простая цель без параметров
reachGoal("contact_form_start");

// Цель с параметрами
reachGoal("contact_form_submit", { service: "legal" });

// Специализированная функция
trackPhoneClick("+74232028878");
```

---

## 📊 Просмотр Данных в Yandex Metrika

### **Где найти цели:**

1. Войдите в Yandex Metrika
2. Выберите счетчик сайта
3. Перейдите в **Отчеты → Стандартные отчеты → Конверсии → Цели**

### **Как настроить цели:**

1. Перейдите в **Настройки → Цели**
2. Нажмите **Добавить цель**
3. Выберите тип: **JavaScript-событие**
4. Укажите идентификатор цели из таблицы выше (например, `contact_form_submit`)

### **Рекомендуемые настройки:**

- **Ценность цели**:
  - `contact_form_submit` - 5000₽ (средняя сделка)
  - `phone_click` / `email_click` - 2000₽
  - `whatsapp_click` / `telegram_click` - 1500₽
  - Остальные - 0₽

---

## 🔍 Отладка в Режиме Разработки

В режиме разработки (`NODE_ENV=development`) доступны debug утилиты в консоли браузера:

```javascript
// Вручную отправить цель
window.analytics.reachGoal("contact_form_submit", { service: "legal" });

// Отследить клик по телефону
window.analytics.trackPhoneClick("+74232028878");

// Отследить клик по email
window.analytics.trackEmailClick("info@uralliance.ru");

// Отследить мессенджер
window.analytics.trackMessengerClick("whatsapp", "footer");

// Посмотреть активные A/B тесты
window.analytics.getActiveABTests();

// Сбросить все A/B тесты (для тестирования)
window.analytics.resetABTests();
```

### **Логирование**

Все события логируются в консоль браузера:

```
[Analytics] ✓ Goal tracked: contact_form_submit { service: 'legal' }
[Analytics] A/B Test "hero_cta": Assigned variant B
```

---

## 📈 Рекомендации по Анализу

### **Воронка конверсии формы:**

1. `contact_form_start` - начали заполнять
2. `contact_form_submit` - успешно отправили
3. `contact_form_error` - получили ошибку

**Метрики:**

- **Коэффициент завершения**: submit / start \* 100%
- **Процент ошибок**: error / (submit + error) \* 100%

### **Сравнение каналов связи:**

Сравните эффективность разных способов связи:

- `phone_click` - кто предпочитает звонить
- `email_click` - кто предпочитает писать
- `whatsapp_click` - кто предпочитает WhatsApp
- `telegram_click` - кто предпочитает Telegram

### **Эффективность прайса:**

- `price_filter_change` - что интересует (legal/tech)
- `price_sort_change` - как ищут (по цене или рекомендации)

---

## 🎨 Кастомизация

### **Добавление новой цели**

1. **Добавьте тип в `src/lib/analytics.ts`:**

```typescript
export type MetrikaGoal = "contact_form_submit" | "your_new_goal"; // <-- добавьте сюда
// ... остальные
```

2. **Используйте в компоненте:**

```typescript
import { reachGoal } from "@/lib/analytics";

const handleClick = () => {
  reachGoal("your_new_goal", { param: "value" });
};
```

3. **Настройте в Yandex Metrika**

---

## ⚙️ Переменные Окружения

Требуется настроить в `.env.local`:

```env
NEXT_PUBLIC_YANDEX_METRIKA_ID=ваш_номер_счетчика
```

---

## 🐛 Решение Проблем

### **Цели не отправляются**

- Проверьте, что установлен `NEXT_PUBLIC_YANDEX_METRIKA_ID`
- Убедитесь, что скрипт Yandex Metrika загружен
- Проверьте консоль браузера на наличие ошибок

### **Цели отправляются дважды**

- Проверьте, что компонент не монтируется дважды (React Strict Mode)
- Используйте `useRef` для предотвращения дубликатов

### **Параметры не передаются**

- Убедитесь, что параметры соответствуют типу `GoalParams`
- Проверьте, что значения не `undefined`

---

**Создано:** Декабрь 2024
**Версия:** 1.0
**Автор:** UrAlliance Development Team
