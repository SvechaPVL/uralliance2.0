# 🚀 Deployment Guide

Полное руководство по настройке автоматического деплоя для UrAlliance.

## 📋 Оглавление

1. [Настройка GitHub Secrets](#настройка-github-secrets)
2. [Настройка SSL/TLS](#настройка-ssltls)
3. [Автоматический деплой](#автоматический-деплой)
4. [Ручной деплой](#ручной-деплой)
5. [Мониторинг](#мониторинг)
6. [Troubleshooting](#troubleshooting)

## 🔐 Настройка GitHub Secrets

### Необходимые Secrets

В настройках GitHub репозитория добавьте следующие secrets:

```
Settings → Secrets and variables → Actions → New repository secret
```

#### SERVER_HOST

```
89.111.153.225
```

#### SERVER_USER

```
root
```

#### SERVER_SSH_KEY

```
Содержимое вашего приватного SSH ключа (~/.ssh/id_rsa)
```

Чтобы скопировать ключ:

```bash
cat ~/.ssh/id_rsa
```

Скопируйте весь вывод, включая заголовки `-----BEGIN ... PRIVATE KEY-----`

## 🔒 Настройка SSL/TLS

После первого деплоя настройте HTTPS с Let's Encrypt:

```bash
# Подключитесь к серверу
ssh uralliance

# Получите SSL сертификат
certbot --nginx -d uralliance.ru -d www.uralliance.ru

# Следуйте инструкциям:
# 1. Введите email для уведомлений
# 2. Согласитесь с ToS (y)
# 3. Выберите опцию 2 (Redirect HTTP to HTTPS)
```

Сертификат будет автоматически обновляться через systemd timer.

Проверка автообновления:

```bash
systemctl status certbot.timer
```

## 🤖 Автоматический деплой

### Как это работает

При каждом пуше в ветку `main`:

1. GitHub Actions билдит Docker образ
2. Загружает образ на сервер
3. Останавливает старый контейнер
4. Запускает новый контейнер
5. Проверяет health check
6. Очищает старые образы

### Проверка статуса деплоя

```bash
# В GitHub репозитории
Actions → Deploy to Production → Latest run
```

## 🛠️ Ручной деплой

Если нужно задеплоить вручную:

```bash
# Подключитесь к серверу
ssh uralliance

# Запустите deployment скрипт
cd /opt/uralliance
./deploy.sh
```

## 📊 Мониторинг

### Проверка статуса контейнера

```bash
ssh uralliance

# Статус контейнера
docker compose -f /opt/uralliance/docker-compose.yml ps

# Логи
docker compose -f /opt/uralliance/docker-compose.yml logs -f

# Последние 100 строк
docker compose -f /opt/uralliance/docker-compose.yml logs --tail=100
```

### Проверка nginx

```bash
ssh uralliance

# Статус nginx
systemctl status nginx

# Тест конфигурации
nginx -t

# Логи nginx
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

### Проверка использования ресурсов

```bash
ssh uralliance

# Использование памяти и CPU
docker stats

# Место на диске
df -h

# Docker образы
docker images

# Очистка неиспользуемых образов
docker image prune -a
```

## 🐛 Troubleshooting

### Контейнер не запускается

```bash
# Проверьте логи
docker compose logs

# Проверьте .env файл
cat /opt/uralliance/.env

# Пересоберите образ
docker compose down
docker compose up -d --build
```

### Сайт недоступен

```bash
# Проверьте nginx
systemctl status nginx
nginx -t

# Проверьте, что контейнер запущен
docker compose ps

# Проверьте порты
netstat -tlnp | grep -E '(80|443|3000)'

# Проверьте firewall (если используется)
ufw status
```

### SSL сертификат не работает

```bash
# Обновите сертификат
certbot renew --dry-run

# Если нужно, получите новый
certbot --nginx -d uralliance.ru -d www.uralliance.ru --force-renewal
```

### GitHub Actions не может подключиться

1. Проверьте, что SERVER_SSH_KEY содержит правильный ключ
2. Убедитесь, что публичный ключ добавлен на сервер:

```bash
ssh uralliance "cat ~/.ssh/authorized_keys"
```

### Место на диске заканчивается

```bash
# Очистите Docker
docker system prune -a --volumes

# Очистите логи nginx
truncate -s 0 /var/log/nginx/*.log

# Очистите журналы systemd
journalctl --vacuum-time=7d
```

## 📁 Структура на сервере

```
/opt/uralliance/
├── .env                  # Переменные окружения
├── docker-compose.yml    # Docker Compose конфигурация
├── deploy.sh            # Скрипт деплоя
└── uralliance-latest.tar.gz  # Docker образ (временный)
```

## 🔄 Обновление конфигурации

### Обновление переменных окружения

```bash
ssh uralliance
nano /opt/uralliance/.env

# После изменений перезапустите контейнер
cd /opt/uralliance
docker compose down
docker compose up -d
```

### Обновление nginx конфигурации

```bash
ssh uralliance
nano /etc/nginx/sites-available/uralliance

# Тест и перезагрузка
nginx -t && systemctl reload nginx
```

## 📝 Полезные команды

```bash
# Быстрый рестарт
ssh uralliance "cd /opt/uralliance && docker compose restart"

# Просмотр переменных окружения
ssh uralliance "docker compose exec uralliance-web env"

# Выполнить команду внутри контейнера
ssh uralliance "docker compose exec uralliance-web sh"

# Backup .env файла
ssh uralliance "cat /opt/uralliance/.env" > .env.backup
```

## 🆘 Поддержка

При проблемах с деплоем:

1. Проверьте логи GitHub Actions
2. Проверьте логи контейнера
3. Проверьте логи nginx
4. Убедитесь, что все secrets настроены правильно

---

**Последнее обновление:** 2025-11-24
