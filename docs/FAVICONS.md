# 🎨 Favicons & PWA Icons

## Структура иконок

### 📁 Файлы в `src/app/`

Next.js автоматически обрабатывает эти файлы:

```
src/app/
├── favicon.ico          # 15KB - Классический фавикон для браузеров
├── icon.svg             # 1.8MB - SVG иконка (масштабируемая)
├── icon.png             # 6.9KB - PNG иконка 32x32
└── apple-icon.png       # 40KB - Иконка для Apple устройств (180x180)
```

**Автоматическая генерация тегов:**
Next.js автоматически добавляет в `<head>`:

```html
<link rel="icon" href="/icon.svg" type="image/svg+xml" />
<link rel="icon" href="/icon.png" sizes="32x32" type="image/png" />
<link rel="apple-touch-icon" href="/apple-icon.png" sizes="180x180" />
```

### 📁 Файлы в `public/`

```
public/
├── manifest.webmanifest          # 1KB - PWA манифест
├── web-app-manifest-192x192.png  # 42KB - Иконка 192x192 для PWA
└── web-app-manifest-512x512.png  # 406KB - Иконка 512x512 для PWA
```

## 🌐 PWA Манифест

**Файл:** `public/manifest.webmanifest`

```json
{
  "name": "Uralliance — Legal & Tech Solutions",
  "short_name": "Uralliance",
  "description": "Юридические услуги и IT-решения для бизнеса",
  "start_url": "/",
  "display": "standalone",
  "theme_color": "#0a0a0a",
  "background_color": "#0a0a0a",
  "icons": [...]
}
```

### Настройки цветов

- **theme_color**: `#0a0a0a` - Темный цвет для браузера (адресная строка на Android)
- **background_color**: `#0a0a0a` - Фон при загрузке PWA

### Размеры иконок

| Размер  | Назначение              | Purpose       |
| ------- | ----------------------- | ------------- |
| 192x192 | Android                 | maskable, any |
| 512x512 | Android, splash screens | maskable, any |

## 📱 Поддержка устройств

### iOS/Safari

✅ `apple-icon.png` (180x180) - используется для:

- Иконка на главном экране
- Touch Bar на Mac
- Иконка в Safari

### Android/Chrome

✅ `web-app-manifest-192x192.png` - используется для:

- Иконка приложения
- Иконка в меню приложений

✅ `web-app-manifest-512x512.png` - используется для:

- Экран загрузки (splash screen)
- Адаптивная иконка высокого разрешения

### Desktop Browsers

✅ `favicon.ico` - классический фавикон
✅ `icon.svg` - современный SVG фавикон (масштабируемый)
✅ `icon.png` - PNG фавикон для старых браузеров

## 🔧 Конфигурация в layout.tsx

```typescript
export const metadata: Metadata = {
  manifest: "/manifest.webmanifest",
  icons: {
    icon: [
      { url: "/icon.png", sizes: "32x32", type: "image/png" },
      { url: "/icon.svg", type: "image/svg+xml" },
    ],
    apple: [{ url: "/apple-icon.png", sizes: "180x180", type: "image/png" }],
  },
};
```

## 🎯 Как Next.js обрабатывает иконки

### Автоматическое обнаружение

Next.js автоматически находит эти файлы в `src/app/`:

- `favicon.ico`
- `icon.{png,svg,jpg}`
- `apple-icon.{png,jpg}`

И генерирует соответствующие теги в `<head>`.

### Приоритет форматов

1. **SVG** - используется если поддерживается браузером
2. **PNG** - фолбэк для старых браузеров
3. **ICO** - фолбэк для очень старых браузеров

## 📊 Проверка работы

### В браузере

1. **DevTools → Elements → `<head>`**
   Проверьте наличие:

   ```html
   <link rel="icon" href="/icon.svg" type="image/svg+xml" />
   <link rel="manifest" href="/manifest.webmanifest" />
   ```

2. **Вкладка браузера**
   Должна отображаться иконка рядом с названием сайта

3. **Закладки**
   При добавлении в закладки должна сохраняться иконка

### PWA

1. **Chrome → Install App**
   При установке PWA должна использоваться правильная иконка

2. **Android → Add to Home Screen**
   Иконка должна корректно отображаться на главном экране

3. **iOS → Share → Add to Home Screen**
   Должна использоваться apple-icon.png

## 🔄 Обновление иконок

Если нужно обновить иконки:

1. Замените файлы в `src/app/` и `public/`
2. Очистите кэш браузера (`Ctrl+Shift+R`)
3. Пересоберите проект: `npm run build`

### Hard Refresh в разных браузерах

- **Chrome/Edge**: `Ctrl+Shift+R` или `Cmd+Shift+R` (Mac)
- **Firefox**: `Ctrl+Shift+R` или `Cmd+Shift+R` (Mac)
- **Safari**: `Cmd+Option+R`

## 📝 Оригинальные файлы

Исходные архивы (не в Git):

- `favicon-for-app.zip` - иконки для src/app/
- `favicon-for-public.zip` - иконки для public/

Эти файлы в `.gitignore`, но хранятся локально для резервной копии.

## ✨ SEO и Best Practices

### Оптимальные размеры

✅ **Используются**:

- 32x32 (favicon.png)
- 180x180 (apple-icon.png)
- 192x192 (PWA)
- 512x512 (PWA)
- SVG (масштабируемый)

### Рекомендации

1. ✅ SVG иконка должна быть максимально оптимизирована
2. ✅ PNG иконки должны быть сжаты (используем оптимизированные)
3. ✅ Используем `purpose: "maskable"` для Android
4. ✅ Указываем правильные MIME типы
5. ✅ Добавляем манифест для PWA

## 🚀 Готово!

Все иконки настроены и автоматически работают во всех браузерах и на всех устройствах.
